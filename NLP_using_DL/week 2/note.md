# WEEK 2 STUDY (2026.7.14-????)

## 2-1 Tokenization
corpus data를 token이라는 단위로 나누는 작업이다.
상황에 따라 token의 단위가 달라지지만 보통은 의미있는 단위로 정의한다.

### 1. Word Tokenization
token의 기준을 word로 하는 경우로, 단어 단위 외에도 단어구, 문자열로도 간주할수도 있다.

토큰화 작업은 구두점이나 특수문자를 제거하고 whitespace를 기준으로 잘라내는 cleaning만으로 해결되지않는다.
whitespace 단위로 token을 구분하면 한국어는 구분이 어려워진다.

### 2. 토큰화 중 생기는 선택
[실습 예제](./2-1.ipynb#2-12-토큰화-중-생기는-선택)

### 3. 토큰화에서 고려해야하는 사항

- 구두점이나 특수문자를 단순히 제외하면 안된다.
문장의 경계를 표시하는 마침표(.)나 날짜를 의미하는 슬래시(/), 가격을 표현하는 달러($)등을 표현하기위해서 조금 더 세부적일 필요가 있다.

- 줄임말과 띄어쓰기를 포함하는 단어가 있는 경우
We're이나 I'm과 같은 clitic이 존재하는 형태나 New York와 같이 띄어쓰기가 포함되는 단어가 존재하기때문에 이러한 단어도 인식할수있어야한다.

- 표준 토큰화 예제
표준 토큰화 규칙
 1) 하이푼으로 구성된 단어는 한 단어로 유지한다.
 2) apostrophe같은 clitic이 포함된 단어는 분리한다.
[실습 예제](./2-1.ipynb#2-13-토큰화에서-고려해야할-사항)

### 4. 문장 토큰화
token의 단위가 sentence일때 이를 구분하는 작업을 문장 분류라고한다.
이때 마침표(.), 물음표(?), 느낌표(!)를 기준으로 자른다면 마침표가 구분자 역할을 수행하지않을 가능성이 있기때문에 조심해야한다.
[실습 예제](./2-1.ipynb#2-14-Sentence-Tokenization)

### 5. 한국어 토큰화의 어려움
영어는 몇몇 합성어나 줄임말의 예외처리만 하면 띄어쓰기를 기준으로 단어 토큰화를 수행해도 문제가 생기지않는다.
하지만 한국어는 띄어쓰기 단위, 즉 어절을 기준으로 토큰화하면 조사, 어미 등이 붙은 교착어이기때문에 제대로된 정제가 어렵다.

- 교착어의 특성
한국어는 조사가 존재한다. 그(he)라는 주어나 목적어가 들어간 문장을 예시로하면 '그가', '그를', '그와', '그는' 등의 조사들이 '그'라는 단어에 붙어있는 형태이다.
서로 다른 조사가 붙으면 다른 단어로 인식 될수도 있기때문에 어절 단위로 토큰화하지않고 조사를 분리해야한다.

>형태소(morpheme): 가장 작은 말의 단위  
>- 자립 형태소: 접사, 어미, 조사와 상관없이 자립하여 사용할 수 있는 형태소. 체언(명사,대명사,수사), 수식언(관형사, 부사), 감탄사 등이 있다.  
>- 의존 형태소: 다른 형태소와 결합하여 사용되는 형태소. 접사, 어미, 조사, 어간이 이에 포함된다.  
>
>ex) 에디가 책을 읽었다  
>1) 단위 토큰화 
>['에디가','책을','읽었다']
>2) 형태소 단위 분해
>자립 형태소: 에디, 책
>의존 형태소: -가, -을, 읽-, -었, -다

- 한국어는 띄어쓰기가 영어보다 잘 지켜지지않는다
한국어는 띄어쓰기가 지켜지지않아도 글을 쉽게 이해할 수 있는 언어이다.

### 6. 품사 태깅
표기는 같지만 품사에 따라 단어의 의미가 달라지기도한다.
예를 들어 명사로서의 '못'과 부사로서의 '못'은 뒤의 동사에따라서 의미가 달라질수있어 품사 구분을 하고, 이 과정을 품사 태깅이라한다.

### 7. NLTK와 KoNLPy를 이용한 토큰화
[실습 예제](./2-1.ipynb#2-17-Tokenization-practice)


## 2-2 Cleaning & Normalization
